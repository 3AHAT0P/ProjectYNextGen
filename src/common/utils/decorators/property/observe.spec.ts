import {
  describe,
  beforeEach,
  expect,
  it,
  jest,
  afterAll,
} from '@jest/globals';

import observe from './observe';

describe('Decorator::observe', () => {
  it('Should ???', () => {
    // nothing
  });
});
