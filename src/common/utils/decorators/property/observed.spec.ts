import {
  describe,
  beforeEach,
  expect,
  it,
  jest,
  afterAll,
} from '@jest/globals';

import observed from './observed';

describe('Decorator::observed', () => {
  it('Should ???', () => {
    // nothing
  });
});
