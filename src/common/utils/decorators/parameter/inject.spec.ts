import {
  describe,
  beforeEach,
  expect,
  it,
  jest,
  afterAll,
} from '@jest/globals';

import inject from './inject';

describe('Decorator::inject', () => {
  it('Should ???', () => {
    // nothing
  });
});
