import {
  describe,
  beforeEach,
  expect,
  it,
  jest,
  afterAll,
} from '@jest/globals';

import last from './last';

describe('Decorator::last', () => {
  it('Should ???', () => {
    // nothing
  });
});
